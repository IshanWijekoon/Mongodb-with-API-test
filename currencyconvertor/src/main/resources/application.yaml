spring:
  application:
    name: currencyconvertor
  mongodb:
    uri: ${MONGODB_URI:mongodb://localhost:27018/currency_db}

server:
  port: ${SERVER_PORT:8082}

app:
  jwt:
    secret: ${JWT_SECRET:change-me-local-dev-secret-min-32-chars!!}
